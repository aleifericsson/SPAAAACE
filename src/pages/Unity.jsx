import { useGame } from "../hooks/useGame";

export default function Unity() {
    const {nearest_planet, show_question, total_score, questions} = useGame;
    
    return (
      <div>

      </div>
    );
  }